import { SortBookingPipe } from './sort-dates.pipe';

describe('SortDatesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortBookingPipe();
    expect(pipe).toBeTruthy();
  });
});
